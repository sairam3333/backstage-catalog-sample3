apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: backstage-node
  description: "backstage testing node"
  annotations:
    github.com/project-slug: sairam3333/backstage-node
spec:
  type: service
  lifecycle: production
  owner: team

  dependsOn:
    - resource:default/mongodb

  providesApis:
    - backstage-node-api
