apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: backstage-angular
  description: "Testing repo for backstage"
  annotations:
    github.com/project-slug: sairam3333/backstage-angular
spec:
  type: service
  lifecycle: production
  owner: team

  dependsOn:
    - resource:default/mongodb

  providesApis:
    - backstage-angular-api
